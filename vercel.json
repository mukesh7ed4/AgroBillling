{
  "buildCommand": "npm run build --configuration production",
  "outputDirectory": "dist/AgroBilling.Client",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}